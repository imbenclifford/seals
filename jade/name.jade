doctype html
html(lang="en")
  head
    link(rel='stylesheet', href='../css/main.css')    
    title= BlogList
  body
    h1 Blog List
  table
      each val, i in message
        li
          h3 #{val.title}
          h4 #{val.user}
          a(href ='quote/#{val.id}') View Post